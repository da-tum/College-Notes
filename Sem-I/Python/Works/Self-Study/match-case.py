#match n case from python 3.10 onw 
#Its used for patters matching 

x=input('enter any number\n')
match x:
    case '1':
        print('one')
    case '2':
        print('two')
    case '3':
        print('three')
    case _:
        print('not in 1,2,3')

# _ is used as default case
# can also use | for or condition
# can also use case 1|2|3: for or condition
# can also use case _ if x>0: for if condition
# can also use case _ if x<0: for if condition
# can also use case _ if x==0: for if condition
# can also use case _ if x%2==0: for if condition
# can also use case _ if x%2!=0: for if condition
# can also use case _ if x.isdigit(): for if condition
# can also use case _ if not x.isdigit(): for if condition
# can also use case _ if len(x)>1: for if condition
# can also use case _ if len(x)==1: for if condition
# can also use case _ if len(x)<1: for if condition
# can also use case _ if x.isalpha(): for if condition
# can also use case _ if not x.isalpha(): for if condition
# can also use case _ if x.isalnum(): for if condition  
# can also use case _ if not x.isalnum(): for if condition
# can also use case _ if x.isspace(): for if condition
# can also use case _ if not x.isspace(): for if condition
# can also use case _ if x.islower(): for if condition
# can also use case _ if not x.islower(): for if condition
# can also use case _ if x.isupper(): for if condition
# can also use case _ if not x.isupper(): for if condition
# can also use case _ if x.istitle(): for if condition
# can also use case _ if not x.istitle(): for if condition
# can also use case _ if x.startswith('a'): for if condition
# can also use case _ if not x.startswith('a'): for if condition
# can also use case _ if x.endswith('a'): for if condition
# can also use case _ if not x.endswith('a'): for if condition
# can also use case _ if x in 'abc': for if condition
# can also use case _ if x not in 'abc': for if condition
# can also use case _ if x.isnumeric(): for if condition
# can also use case _ if not x.isnumeric(): for if condition.
# can also use case _ if x.isdecimal(): for if condition
# can also use case _ if not x.isdecimal(): for if condition
# can also use case _ if x.isidentifier(): for if condition
# can also use case _ if not x.isidentifier(): for if condition
# can also use case _ if x.isprintable(): for if condition
# can also use case _ if not x.isprintable(): for if condition
# can also use case _ if x.isspace(): for if condition
# can also use case _ if not x.isspace(): for if condition
# can also use case _ if x.isascii(): for if condition
# can also use case _ if not x.isascii(): for if condition
# can also use case _ if x.isdecimal(): for if condition
# can also use case _ if not x.isdecimal(): for if condition
# can also use case _ if x.isdigit(): for if condition
# can also use case _ if not x.isdigit(): for if condition
# can also use case _ if x.isnumeric(): for if condition
# can also use case _ if not x.isnumeric(): for if condition
# can also use case _ if x.isidentifier(): for if condition
# can also use case _ if not x.isidentifier(): for if condition
# can also use case _ if x.isprintable(): for if condition
# can also use case _ if not x.isprintable(): for if condition
# can also use case _ if x.isascii(): for if condition
# can also use case _ if not x.isascii(): for if condition
# can also use case _ if x.isspace(): for if condition
# can also use case _ if not x.isspace(): for if condition
# can also use case _ if x.islower(): for if condition
# can also use case _ if not x.islower(): for if condition
# can also use case _ if x.isupper(): for if condition
# can also use case _ if not x.isupper(): for if condition
# can also use case _ if x.istitle(): for if condition
# can also use case _ if not x.istitle(): for if condition
# can also use case _ if x.startswith('a'): for if condition
# can also use case _ if not x.startswith('a'): for if condition
# can also use case _ if x.endswith('a'): for if condition
# can also use case _ if not x.endswith('a'): for if condition
# can also use case _ if x in 'abc': for if condition
# can also use case _ if x not in 'abc': for if condition
# can also use case _ if len(x)>1: for if condition
# can also use case _ if len(x)==1: for if condition
# can also use case _ if len(x)<1: for if condition
# can also use case _ if x.isalpha(): for if condition
# can also use case _ if not x.isalpha(): for if condition
# can also use case _ if x.isalnum(): for if condition

